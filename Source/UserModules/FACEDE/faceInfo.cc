#include faceInfo.h
void faceInfo::Init()
{
size= GetInputSize("FACES")
input1 = GetInputArray("FACES");
}
void faceInfo::Tick(){
	int j=0;
	for(int i=0; i<size; i++)
		if(j=<3){
			j=0;

	printf("%s\n", );
		j++;

}